<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="text-zinc-200">
    <div class="main w-full min-h-screen bg-zinc-900">

        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block w-full outline-none rounded-md px-3 py-2 bg-zinc-800" type="text" placeholder="Title gose here.." name="title">
                <textarea class="block w-full outline-none rounded-md mt-2 px-3 py-2 resize-none bg-zinc-800" placeholder="Write your task details here" name="details"></textarea>
                <input class="mt-3 cursor-pointer bg-blue-600 px-4 py-2 rounded-md active:scale-98" type="submit" value="Create Task">
            </form>
        </div>

        <div class="tasks p-10 flex gap-4 flex-wrap">
            <% if(files.length > 0) { %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-5 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-zinc-100 text-[20px] tracking-tighter"><%= val %></h1>
                        <a class="text-blue-500 inline-block mt-3" href="">Read More.</a>
                    </div>
                <% }) %>
            <% } else { %>
                <h1 class="text-zinc-600">No tasks yet.</h1>
            <% } %>
        </div>
    </div>
</body>
</html>